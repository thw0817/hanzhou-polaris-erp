FROM node:22-alpine

WORKDIR /app
ENV NODE_ENV=production

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY server ./server
COPY deploy/postgres ./deploy/postgres
COPY src ./src
COPY src-v2 ./src-v2

USER node
EXPOSE 8790 8791

CMD ["npm", "run", "cloud:control"]
