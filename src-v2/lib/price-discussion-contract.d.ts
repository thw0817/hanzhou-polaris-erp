export function skuSizeLabel(values: string[]): string;
export function pricePerSquareMeter(price: number | null, values: string[]): number | null;
export function formatDiscussionMoney(value: number | null, currency: string): string;
